#!/usr/bin/env bash
# Run the TUI step: the example, then the tests.
set -euo pipefail

cd "$(dirname "$0")/../agent/11_tui"
uv run examples/example.py "$@"
echo
exec uv run python -m unittest discover -s tests -t .
